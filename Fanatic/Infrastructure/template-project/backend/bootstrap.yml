spring:
  profiles:
    active: default
  cloud:
    config:
      uri: $CONFIG_URI
#       name: ${PROJECT_CONFIG_FILE},common

# logging:
#   path: logs
#   logstash:
#     host: ${LOGSTASH_HOST}
#     appname: ${LOGSTASH_APPNAME}
