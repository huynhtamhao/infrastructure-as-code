spring:
  profiles:
    active: default
  cloud:
    config:
      uri: http://$SERVER_URL:$CONFIG_PORT
      name: $PROJECT_CONFIG_FILE,common

logging:
  path: logs
  logstash:
    host: $SERVER_URL:$LOGSTASH_PORT
    appname: $LOGSTASH_APPNAME
