spring:
  profiles:
    active: default
  cloud:
    config:
      uri: '${URI}:8888/'
      name: usermanagement,common

logging:
  path: logs
  logstash:
    host: 192.168.210.201:5044
    appname: ユーザー管理スシステム
