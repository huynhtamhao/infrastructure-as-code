version: "3"
services:
  app:
    container_name: "be_config"
    build: .
    ports:
      - 8888:8888
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
    volumes:
      - .:/usr/src/app
